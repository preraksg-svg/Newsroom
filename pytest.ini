[pytest]
# The hermetic unit/integration suite lives in tests/. The two legacy scripts at
# the repo root (test_email.py, test_scrape.py) are manual, network-dependent
# tools, not part of the automated suite.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
