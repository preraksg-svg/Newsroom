name: Keep Alive Zapway Newsroom

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://newsroom-1zapway-newsroom-cloud.onrender.com/api/news > /dev/null
